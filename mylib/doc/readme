Пример статической библиотеки.

В дереве исходных кодов "открытые" - предназначенные для явного включения в пользовательские исходные тексты - заголовочные файлы
размещаются в каталоге include. Исходные тексты библиотеки, а также "внутренние" заголовочные файлы находятся в каталоге src.

Сборка библиотеки:
    cd <каталог mylib>
    make
Очистка каталога:
    make clean
